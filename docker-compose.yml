services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./output:/app/output
      - ./logs:/app/logs

  bot:
    build: .
    command: python -m app.bot
    env_file:
      - .env
    depends_on:
      - api
